# resolv.conf do contêiner ZoneT_01 — resolvedor único é o dnscrypt-proxy local.
# Garante que nenhuma consulta DNS escape para o host ou para a rede.
nameserver 127.0.0.1
options no-tld-query timeout:5 attempts:2
